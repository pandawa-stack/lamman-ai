{
  "name": "lamman-ai",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.24.0",

  "scripts": {
    "dev": "turbo dev",
    "dev:all": "turbo dev --parallel",
    "build": "pnpm --filter web build"
  },

  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
