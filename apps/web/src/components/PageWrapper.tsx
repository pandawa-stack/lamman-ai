// File: apps/web/src/components/PageWrapper.tsx
'use client';

// Komponen wrapper sederhana tanpa logika

export default function PageWrapper({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}